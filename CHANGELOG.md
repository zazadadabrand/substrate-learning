# Changelog

## v13
Modules ship at commission. The curriculum pass stays a compact skeleton. A second materials pass, one layer at a time and then smaller batches for anything that came back empty, writes a lesson, a reference sheet, and a drill onto every atom before the track opens. Existing commissioned tracks keep their atoms and known-state. Opening one that has no modules starts the materials pass in place. It does not regenerate the curriculum, so progress keys stay put. Commissioned tracks open in the same shell as Creative Director. The module button is attached there when an atom already has a module. Creative Director and `substrate-cd-v5` are unchanged.

## v12
Commission engine: structured tool_use, 8192 max tokens, JSON extract/repair/salvage so a truncated or messy first pass still populates layers instead of dying on ENGINE_PARSE. Client opens the generated track when a commission succeeds. Quote/stack overlap pad on commissioned cards stays.

## v8
Runtime hardening: storage API fallback shim, load order fix for the audio engine, mini stack variable fix. Simulated browser boot test added to the ship process.

## v7 series
Bernard Studia brand system: site palette (navy base, orange to pink to violet to blue ombre), Archivo Black jumbotron type with the two tone white plus gradient hero move, Fraunces wordmark eyebrows, marquee tickers, numbered layer grammar, blown out LED pixel field background. Kid simplicity pass across all lesson copy per the Feynman mandate.

## v6 series
Multi track shell: home menu of substrate stacks with per track overall tracking, isometric mini stack graphics with progress fill, pending track slots, commission a track slot. Material 005 Color Harmonies shipped: lesson, reference, generated palette drill.

## v5 series
Material system: Material 001 Typeface Classification with from zero lesson, embedded checks, contrasting cases, reference tab, timed pass drill. Browser native audio on every lesson step with voice picker. Guided exercise progressions and explicit pass conditions on all 49 atoms.

## v4
Time projections: hour estimate per atom, pace selector, completion date projection, per layer remaining hours. Readings per layer. Bloom protocol defined.

## v3
Cohort driven restructure from the nineteen references: five layers including the new Object and Space layer, The Nineteen as the measure in the top layer, studio code atom.

## v2
Deterministic rebuild: 32 hard skill atoms, binary know or not states, daily rep, gap logs.

## v1
First map: isometric five layer stack, guided atoms, persistent progress.
